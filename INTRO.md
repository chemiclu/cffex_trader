# cffex-trader · 基于东方财富模拟交易 Skill 的多因子轮动编排框架

> **GitHub 仓库：** <!-- 待发布后填写链接 --> `(链接待补)`
>
> **仅供模拟交易 / 学习研究使用，不构成投资建议。** 期货交易风险极高，请勿接入真实资金。

---

## 一句话介绍

**cffex-trader** 是一个构建在「东方财富期货·中金所杯模拟交易 skill」（dfcfqh-zjsbsimrace）之上的**交易编排增强框架**，为 Claude Code skill 生态设计。它在东方财富官方 skill 的「账户查询 + 模拟下单」原子能力之上，补齐了从**数据 → 信号 → 执行 → 风控 → 复盘**的完整量化交易闭环，覆盖中金所全部 8 个期货品种：

| 组别   | 品种                                      |
| ---- | --------------------------------------- |
| 股指期货 | IF（沪深300）、IH（上证50）、IC（中证500）、IM（中证1000） |
| 国债期货 | TS（2年）、TF（5年）、T（10年）、TL（30年）            |

---

## 基于东方财富交易 Skill 的增量优化

东方财富官方 skill 提供以下能力：账户/持仓/委托/成交查询、下单、撤单、排名与赛程查询。cffex-trader 在其上做了以下增量：

### 1. 三阶段编排闭环

把「更新数据 → 生成信号 → 执行下单」串成一条命令链，一次调仓不再需要逐个手动调用接口，且每一步产出（`signal.json`、归档日志）都可供下一步与复盘使用。

### 2. 支持自定义策略添加（用户自备）

- 可导入第三方策略，配和skill下单使用
- 目前支持以下数据接口：

#### 本地 SQLite 日线行情

| 字段                        | 可派生因子                     |
| ------------------------- | ------------------------- |
| open / high / low / close | 动量、均线/趋势、ATR、突破、日内形态      |
| **volume**                | 量能变化、量价背离、放量确认            |
| **hold（持仓量）**             | 增减仓趋势、持仓量×价格共振（期货特有）      |
| **settle（结算价）**           | close/settle 偏离、以结算价口径的收益 |
| amount（成交额）               | 流动性/活跃度过滤                 |

#### 国债收益率

- `ak.bond_zh_us_rate` 提供中债国债收益率曲线 **2/5/10/30 年** → 可派生 Carry、期限利差（如 10Y−2Y）、收益率动量。

#### 消息面情绪

#### 实时盘口

- 下单阶段自动拉五档 ask1/bid1。框架只用于对手价定价，但同一 API 也能取日内 tick/分钟线，自定义引擎可用它做**盘口失衡、日内动量**等高频因子（需自己在引擎里调整）

#### 注意

- 若数据源缺失，将自动降级：Wind → akshare → 中性值，任一环节失败不中断流程。
- ⚠️ **策略引擎 `futures_multifactor_engine.py` 不随仓库分发**，用户从私有渠道获取后手动放入目录（README 提供最小接口规范：5 个符号 + 1 个函数即可接入）。框架与策略解耦——你也可以写自己的引擎。

### 3. 增量调仓（对照持仓，只下差异腿）

下单前实时查询当前持仓，与目标持仓双向对比：

| 对比结果    | 处理           |
| ------- | ------------ |
| 无持仓     | 开仓           |
| 同向且已到位  | 跳过           |
| 同向不足    | 差额加仓         |
| 反方向     | 先平旧仓 + 反向开新仓 |
| 持仓在、目标外 | 自动平旧腿        |

先平后开（先释放保证金再开新仓），彻底避免重复下单、误加仓、旧腿越积越多。

### 4. 实时盘口对手价（TqSdk 免费行情）

收盘参考价跨交易日会过时。下单前自动拉实时五档：**买报卖一、卖报买一**（对手价保证成交），拉不到时静默回退参考价并标注。

### 5. 自动换月检测

- **映射失效**：主力合约映射指向已退市合约时，`orders` **硬阻断拒绝下单**并给出建议新月份（实战中曾第一时间检出股指 2609 全系退市）。
- **到期预警**：按中金所交割规则估算交割日（股指=当月第三周周五、国债=第二周周五），≤5 天报警。
- **持仓跨月**：平仓单自动用**持仓实际月份合约**（从持仓中文名还原），换月过渡期旧仓正确滚动、止损不打错月份。

### 6. 软件侧自动止损

模拟接口不支持交易所侧条件单，本框架用软件盯盘补齐：期货总浮亏 ≤ −8 万 **或** 资金使用率 ≥ 95%（均可调），任一触发即按实时持仓方向自动平掉全部仓位（空头买平、多头卖平）。每次下单后自动挂载，`stop-watch` 可接定时任务。

### 7. 消息面情绪因子（可选）

接入东方财富**妙想资讯搜索**（mx-search skill）：按股债分组拉最新资讯，关键词规则算组级情绪 ∈ [-1,1]，以可调权重（默认 15%）并入综合得分。查询失败自动降级纯量化、不被中性值稀释；当日结果缓存；旧闻/科普文时效过滤。

### 8. 保证金手数折算

按「价格 × 合约乘数 × 交易所保证金比例」折算每腿手数，保证多空各 2 腿等权、每腿至少 1 手；目标使用率可调（默认 90%，激进档预留 8% 安全垫避开止损线）。

### 9. 风险监控与爆仓预案

`risk` 命令一键输出：权益、使用率、距预警/强平/穿仓的再亏损空间、逐腿「怕涨/怕跌」方向与每点盈亏。分级预案：🟢 周度调仓 → 🟡 每日查账户 → 🟠 主动减仓 → 🔴 立即清仓保本。

### 10. 信号反馈闭环与复盘归档

- `signal_log.jsonl` 归档每次信号快照，`report` 命令回放**多空方向命中率**与等权组合收益（命中率 <50% 提示因子失效）。
- `trade_log.jsonl` 归档每次真实委托，与信号日志配合复盘「信号 → 下单 → 盈亏」全链路。
- 下单后自动把账户快照 + 当日委托 + 最新持仓同步进 Markdown 交易日志（同日幂等覆盖）。

### 11. `doctor` 部署自检 + 个性化教程生成

一条命令 7 项检查（策略引擎 → Python 依赖 → 环境变量 → 上游 skill → 数据库 → 接口连通 → 换月映射），每项失败给出修复命令；首次运行按**当前机器状态**生成 `GETTING_STARTED.md` 上手教程，并自动提醒需要安装的前置 skill。

### 12. 安全设计（不可跳过的铁律）

1. `orders` 默认 **dry-run** 只预览；`--yes` 还需输入 `y` 二次确认。
2. 交易失败/超时后**禁止自动重试、改价、补单**，只允许查询状态。
3. 平仓前强制重查持仓方向。
4. 全部凭据走环境变量，仓库零硬编码（见 SECURITY.md 自查清单）。

---

## 快速上手（3 分钟）

```bash
# ⓪ 获取策略引擎（不随仓库分发）并放入本目录
#    最小接口见 README「策略引擎获取」；框架自带 doctor 会检查它

# ① 安装依赖
pip install pandas requests akshare tqsdk

# ② 配置环境变量（写入 ~/.bashrc 持久化）
export CFAPIKEY="<东方财富模拟交易用户ID>"   # 必需
export TQSDK_USER="<快期账号>"               # 可选：实时盘口
export TQSDK_PASS="<快期密码>"

# ③ 部署自检（首次运行生成 GETTING_STARTED.md 个性化教程）
python orchestrator.py doctor

# ④ 日常使用
python orchestrator.py update       # 更新 8 品种 OHLC
python orchestrator.py signal       # 生成多空信号（--no-news 跳过消息面）
python orchestrator.py orders       # 预览增量委托（dry-run）
python orchestrator.py orders --yes # 确认后下单（输入 y 二次确认）
```

常用辅助命令：

```bash
python orchestrator.py holdings     # 持仓
python orchestrator.py account      # 资金/权益
python orchestrator.py risk         # 强平边界 + 逐腿风险
python orchestrator.py stop         # 止损状态 + 一次性检查
python orchestrator.py report       # 信号命中率回放
```

---

## 前置依赖

| 依赖                            | 作用                     | 必需?  |
| ----------------------------- | ---------------------- | ---- |
| 东方财富 dfcfqh-zjsbsimrace skill | 账户查询 + 模拟下单（本框架的执行层）   | ✅    |
| futures-trader skill          | 期货 OHLC 本地 SQLite（数据层） | ✅    |
| mx-search skill               | 妙想资讯 → 消息面因子           | ⭕ 可选 |
| TqSdk 免费账户（shinnytech 注册）     | 实时五档行情                 | ⭕ 可选 |

所有路径均可通过 `CFFEX_*` 环境变量覆盖，详见各文件头部注释。

## 适用人群

- 参加「中金所杯」模拟交易赛、想要**系统化/自动化**交易流程的学生选手
- Claude Code skill 用户：本仓库自带 `SKILL.md`，克隆进 skills 目录即可对话式调用

## 免责声明

本项目为**模拟交易**工具，所有收益数据不代表真实收益；不构成任何投资建议。因子方向样本期敏感，使用前请自行回测验证。软件止损依赖盯盘进程在线，App 关闭期间不生效。请勿接入真实资金。

## License

MIT
